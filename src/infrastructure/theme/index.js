import { colors } from "./colors";
import { space, lineHeights, spaceVal } from "./spacing";
import { sizes } from "./sizes";
import { fonts, fontWeights, fontSizes } from "./fonts";
import { borderWidth } from "./border-widths";
import { cornerRadius } from "./corner-radius";
import { letterSpacing } from "./letter-spacing";

export const theme = {
  colors,
  space,
  spaceVal,
  lineHeights,
  sizes,
  fonts,
  fontSizes,
  fontWeights,
  borderWidth,
  cornerRadius,
  letterSpacing,
};
